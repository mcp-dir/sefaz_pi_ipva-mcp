# Ferramentas

SEFAZ PI: IPVA expõe 1 ferramenta (todas somente leitura).

### 1. `sefaz_pi_ipva_consultar`
**Input**: `cpf` (opcional), `cnpj` (opcional), `renavam` (opcional), `chassi` (opcional), `tipo_cotas`

SEFAZ PI: IPVA, consulta em fonte oficial.

## Prompts de exemplo

```
Consultar SEFAZ PI: IPVA
```
